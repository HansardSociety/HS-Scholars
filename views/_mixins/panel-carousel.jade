mixin panelCarousel( $headerFields, $carouselItems  )
  .PanelCarousel

    //--
      - Block for panel header mixin.
      - Nested mixin entry fields must derive from the parent mixin,
      - hence the $headerFields variable, which is simply used to
      - set variables for nested fields. 
      --
    block panelHeader

    .PanelCarousel-carousel

      //--
        - Prev and next arrows:
        - Generated by Slick carousel - see custom.js.
        --

      //-- Carousel items --
      each $item in $carouselItems
        .PanelCarousel-carouselItem
          block panelCarouselItem